import type { FC } from "react";
import styles from './Home.module.scss';

const Home: FC = () => {
    return (
        <section className={styles.homePage}>
            home
        </section>
    )
}

export default Home;