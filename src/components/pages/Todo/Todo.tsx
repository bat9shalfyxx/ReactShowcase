import type { FC } from "react";
import styles from './Todo.module.scss';

const Todo: FC = () => {
    return (
        <section className={styles.todoPage}>
            todo
        </section>
    )
}

export default Todo;